# Ensure scoped packages are always public by default
@hydric:registry=https://registry.npmjs.org/
access=public

# Enable Provenance for the "Verified" badge (NPM 9.5+)
provenance=true

# Authentication for CI/CD environments
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
